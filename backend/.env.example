PORT=5000
DATABASE_URL="postgresql://postgres:password@localhost:5432/doctor_clinic?schema=public"
JWT_SECRET="your-jwt-secret-here"

# Optional SMTP settings for email dispatch (simulated if SMTP_USER is unset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=